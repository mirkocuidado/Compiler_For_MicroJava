program p
	const int size = 10;
	{
	
	void f(char ch, int a, int arg)
	int x;
	{
		x = arg;
	}
	
	int pppp(){
		return 0;
	}
	
	
	void main() int x, i; char c;
	{
	
		pppp();
		i = 0;
		do{
			 i++;
		}while(i<size);
		
		f(c, x, i);
		read(x);
		do{
	 		if(x < 0) break;
	 		if (0 < x && x < size){
				print(1);
			}
			else if (-size < x && x <= 0){
				print(2);
	 		}
			read(x);
	 	}while(true);
	 	
	print(true ? '-' : '+');
	}
}